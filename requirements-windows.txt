# Windows 10 / Python 3.10 reference environment.
# For CUDA 12.6 wheels, install PyTorch from the official cu126 index, for example:
# python -m pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu126
# Then install this file with: python -m pip install -r requirements-windows.txt

torch==2.6.0
torchvision==0.21.0
numpy
Pillow
timm
